version: "3.9"
services:
  api:
    build:
      context: ./backend
    env_file:
      - .env
    volumes:
      - ./backend/app:/app/app
      - ./data:/app/data
    ports:
      - "8000:8000"
  web:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
